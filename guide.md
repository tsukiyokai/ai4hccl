# AI辅助HCCL通信算子组研发效率提升全流程指导书

| 项目     | 内容                                          |
| -------- | --------------------------------------------- |
| 文档编号 | HCCL-AI-GL-001                                |
| 版本     | v2.0                                          |
| 状态     | 建议稿                                        |
| 日期     | 2026-03                                       |
| 适用范围 | HCCL集合通信库(cann-hccl、cann-hcomm)开发团队 |

## 摘要

本指导书针对HCCL集合通信库的业务特点，基于Brooks本质/偶然复杂性理论框架，结合repo-dig缺陷挖掘的实证数据和conv算子组的实战复盘经验，系统分析了AI辅助研发在8个环节（需求分析、详细设计、编码、构建、代码检视、开发者测试、上板验证、问题定位）中的适用性与边界，并提出八条AI辅助基本策略。

## 术语与缩略语

| 缩略语     | 全称                                    | 说明                                                |
| ---------- | --------------------------------------- | --------------------------------------------------- |
| HCCL       | Huawei Collective Communication Library | 华为集合通信库                                      |
| HCOMM      | HCCL Communication底层库                | HCCL底层通信实现，约55万行                          |
| God Object | —                                       | 职责过重的大型类/文件，本文特指超过3000行的热点文件 |
| Risk Tier  | —                                       | AI自治度分级                                        |
| TSan       | ThreadSanitizer                         | 线程安全检测工具                                    |
| ASan       | AddressSanitizer                        | 内存错误检测工具                                    |
| OpCode     | Operation Code                          | HCCL通信操作码，涉及版本兼容性                      |
| MAP        | Mean Average Precision                  | 信息检索评价指标                                    |
| SE         | System Engineer                         | 系统工程师                                          |
| MDE        | Module Design Engineer                  | 模块设计工程师                                      |
| UT         | Unit Test                               | 单元测试                                            |
| LLT        | Low Level Test                          | 底层测试                                            |
| CI         | Continuous Integration                  | 持续集成                                            |

## 总论

### 背景与目标

HCCL是昇腾AI处理器生态的集合通信基础设施，支持单机多卡到32K卡的分布式训练。代码规模约65K行（HCCL主仓）+55万行（HCOMM底层库），涉及13种集合通信算子、多级通信拓扑、多种通信算法的自适应选择。

本指导书旨在：识别HCCL 8个研发环节中AI能有效介入的切入点，给出可落地的策略和工具建议，并明确AI不应触碰的边界。

### HCCL与周边算子组的复杂性差异分析

conv算子组在AI辅助研发上已有实战复盘，最终代码约70%以上由AI生成。但HCCL不宜直接套用周边算子组的经验，两者在复杂性结构上存在根本差异。

表1 HCCL与周边算子组复杂性对比

| 维度         | 周边算子组(ops)                            | HCCL通信库                         | 对AI策略的影响                                      |
| ------------ | ------------------------------------------ | ---------------------------------- | --------------------------------------------------- |
| 代码模式     | 算子间高度相似（tiling/kernel/host三件套） | 13种通信算法逻辑各自独特           | ops适合模板仿写（实测最大提效点），HCCL仿写空间有限 |
| 核心困难     | 精度、NPU硬件约束、向量化                  | 并发正确性、分布式一致性、协议兼容 | HCCL的核心困难更难被AI消除                          |
| 样板代码占比 | 高（CMake/host/tiling模板）                | 低（逻辑代码占比高）               | HCCL中AI可消除的偶然复杂性比例更小                  |
| 代码规模特征 | 单算子规模小（数百行kernel）               | 单文件规模大（God Object达9152行） | HCCL的AI辅助受上下文窗口限制更为严重                |
| 缺陷可审查性 | 95%（模式化缺陷为主）                      | 85%（并发类缺陷隐蔽）              | HCCL需补充并发专项审查，AI覆盖率上限更低            |
| 测试特点     | 单卡UT/精度对比                            | 多卡集合通信（需物理设备）         | HCCL测试环境搭建的自动化价值更高                    |

核心差异：周边算子组的复杂性中约60%属于偶然复杂性（模板化、重复性工作），AI可消除其中大部分；HCCL各环节偶然复杂性占比从10%到70%不等，按工作量加权后约30%，其中编码和检视环节偶然占比仅35%和15%，AI无法触及并发控制、分布式一致性和协议设计等核心本质复杂性。

### 理论框架：本质复杂性与偶然复杂性

Brooks在"No Silver Bullet"中将软件困难划分为两类：

- 本质复杂性(essence): 问题域固有的复杂度，无法通过工具消除。对应HCCL中的并发控制、通信算法设计等。
- 偶然复杂性(accident): 当前工具和流程引入的额外摩擦。对应CMake脚本维护、测试脚本编写、日志格式化等。

AI作为工具，主要消除的是偶然复杂性。软件工程历史上三次最大的生产力提升（高级语言、分时系统、统一编程环境）同样如此。AI并非银弹，而是又一次对偶然复杂性的消除。

### 总体策略

基于上述分析，HCCL的AI辅助策略应采取精准投放模式——集中资源到偶然复杂性密集的环节，而非全面铺开。

## 通用约束

以下8条约束适用于所有环节，是逐环节约束的基础。C1-C6为理论约束（源自Brooks框架和HCCL实证数据），C7-C8为流程约束（源自团队流程讨论）。

核心理念：在本质复杂性占70%的系统中，"知道AI不该做什么"比"知道AI能做什么"更关键。AI在HCCL中的最大风险不是"没用好"，而是"用错了地方"。

1. C1 偶然复杂性占比低于20%的环节，AI仅限信息整理，不参与决策
2. C2 凡涉及正确性判断，必须有确定性验证手段；AI概率性意见不可作为最终判据
3. C3 变更爆炸半径越大，AI自治度越低
4. C4 AI遗漏的问题必须持久化——可规则化的入规则库，不可规则化的入checklist
5. C5 向AI提供上下文：200-500行精准相关代码，不喂整个文件
6. C6 生成后未经编译验证的代码视为负债
7. C7 环节衔接：每个环节的产出必须包含AI可消费的格式，为下一环节的AI辅助提供输入
8. C8 AI无合入权限——不给予AI +1/+2权限；AI的所有输出必须经人工确认后才产生流程效力

## 逐环节AI辅助策略

### 需求分析

- 责任方：SE
- 输入：业界论文、技术趋势、竞品分析、客户需求
- 输出：需求分析文档（背景、目标、达成路径）

与上游的衔接：无（研发链起点）。

本环节产出对下游的约束：需求文档是详细设计环节AI辅助的输入源。

前置条件：

- 无特殊前置。需求分析是研发链起点，AI可直接参与信息整理工作。

红线：

1. AI不得参与技术判断——MPI语义适配、拓扑约束理解、性能指标推导、竞品技术路线选择，均由SE完成
2. AI输出的竞品对比/论文摘要仅作参考材料，不得作为需求决策依据

产出格式约束：

1. 需求分析文档必须有一份md格式
2. 需求分析文档里面的图使用PlantUML绘制
3. 需求分析文档里面包含接口定义
4. 需求分析文档里面包含参考资料，用于辅助后续详细设计文档生成

度量指标：

- AI辅助竞品分析的资料整理效率提升（人工整理时间对比）
- 需求文档中结构化约束的覆盖率

### 详细设计

- 责任方：MDE
- 输入：需求分析文档
- 输出：详细设计文档

与上游的衔接：消费需求分析文档（md+PlantUML+接口定义+参考资料）。上游产出不符合格式要求 → AI辅助设计效果打折。

本环节产出对下游的约束：详细设计文档是编码环节AI辅助的核心输入。

前置条件：

1. 需求分析文档已以md格式交付且包含接口定义——否则AI生成设计骨架缺乏输入
2. 需求文档中的并发安全不变量已明确——否则AI生成的设计骨架缺乏判断基准

红线：

1. AI生成的设计方案不得直接采纳——仅作思考起点
2. 并发设计规格（锁粒度、同步原语、屏障位置）不由AI决定
3. AI生成的时序图中的同步点必须人工校验——AI可能遗漏关键同步点

产出格式约束：

1. MDE输出的设计文档必有一份md格式
2. MDE输出的详细设计文档必须有PlantUML的源码，便于下一阶段开发人员用于分析和编码
3. STC评审的时候必须严格把关正常和异常分支覆盖100%——这些分支是编码阶段AI生成UT和代码的判断基准；分支覆盖不全 → 编码阶段AI生成的代码必然有遗漏

度量指标：

- 设计文档编写时间变化（预期图表绘制时间-70%，文档结构化时间-30%）
- AI辅助各章节的准确率（预期：格式/结构类>80%，并发/协议设计类~50%）

### 编码

- 责任方：开发人员 + IDE
- 输入：详细设计文档
- 输出：功能代码 + UT + LLT

编码是本质复杂性和偶然复杂性并存的环节。

与上游的衔接：消费详细设计文档（md+PlantUML源码+正常/异常分支全覆盖）。设计文档是AI编码的"方案说明书"——没有它AI只会更快地生成错误代码。

本环节产出对下游的约束：代码+UT+AI检视报告是代码检视环节的输入。

前置条件：

1. 详细设计文档已完成并review通过（含STC评审的正常/异常分支100%覆盖）——不跳过设计直接让AI编码
2. 如涉及God Object（3个热点文件），已按功能分区准备好200-500行精准上下文+接口签名；禁止整体输入
3. 错误处理模式库已建立(C-4)——AI引用标准模式，不自行创造新模式
4. 远程编译环境可用——每次生成后须即时编译验证

红线：

1. 四个AI编码禁区（硬性）：并发控制逻辑/通信算法实现/OpCode版本协商/状态机转换逻辑——AI不得生成这四类代码的实现（可生成接口签名和注释占位）
2. 控制AI单次生成的代码量，避免大段生成；每次生成后须：编译通过 + 相关UT通过 + 人工review并发相关代码
3. 并发代码变更须附带TSan验证结果(Beyond Done)——无TSan证据的并发修改不得合入
4. AI不得自行创造新的错误处理模式——新模式需经code review确认后入库
5. AI不得判断哪些错误需要重试、哪些需要立即失败——此属通信库业务语义决策

TDD约束：

1. 遵循TDD先行原则：先让AI写UT，UT一旦人工确认通过，后面开发过程中禁止AI再修改UT目录——UT是正确性的锚点，不能随着实现代码一起漂移
2. AI生成的UT必须人工检视确认——AI可能遗漏边界条件或生成形式上通过但实质上不验证任何东西的UT
3. AI生成的Checker必须人工检视确认

PR前约束：

4. 开发者在推送PR前，必须使用AI进行检视，并确认AI的检视意见
5. 开发人员在提交PR前除了AI检视，必须要在本地进行人工检视——AI检视不替代人工检视，两者都是必要条件

判断标准：AI可以写的代码 = 换一个不懂并发的程序员照着模板也能写出来的代码。需要理解线程交互、分布式一致性、协议演进的代码 = AI禁区。

度量指标：

- 功能代码中AI生成占比（预期30-40%），其中样板代码AI占比>80%，核心逻辑AI占比<10%
- 编码阶段因并发问题导致的返工次数
- 每次AI生成后的编译通过率

### 代码检视

- 责任方：开发人员（多人）
- 输入：代码
- 输出：修正后代码

与上游的衔接：消费编码环节产出的代码+UT+AI检视报告。编码环节已要求PR前做AI检视+本地人工检视，代码检视环节是第二道防线。

前置条件：

1. vibe-review已配置HCCL项目级规则（80条）——否则Layer 1自动检视无效
2. 对P0变更（涉及并发控制）：TSan/ASan基础设施可用——确定性证据 > 概率性意见

红线：

1. AI检视结论不得替代TSan/ASan验证——AI说"并发安全"不算数，TSan说"无竞态"才算数
2. AI不得做最终的并发逻辑正确性判断——Layer 3（并发/跨层/协议）必须由人工完成
3. AI标注的可疑并发问题，即使人工认为"没问题"，也须附加理由记录——防止Eagerness Paradox

责任链约束：

1. AI提出的检视结果必须由开发人员进行确认；最终committer检视或+1的时候必须确认开发人员的反馈结果
2. committer必须对最后一个节点生成的AI检视报告负责——人工确认最后存留的AI检视意见都是误报或已知遗留
3. 开发人员/committer发现AI误报的问题，必须反馈@fanshanshan，用于AI经验积累（Learning Paradox的具体闭环机制）
4. 不给予AI +1/+2权限——AI无合入决策权(C8)

判断标准：AI检视的成功标准不是"发现了多少bug"，而是"释放了reviewer多少注意力给Layer 3"。如果reviewer仍在花时间看格式/规范问题 → Layer 1未达标。

度量指标：

- AI检视时长vs人工检视时长
- review准确率（目标>=80%）

### 开发者测试

- 责任方：开发者 + 测试框架
- 输入：功能代码
- 输出：UT + LLT(checker) + 测试报告

与上游的衔接：消费编码环节产出的代码。注意：在TDD模式下，UT实际上在编码阶段已由AI先行生成并经人工确认冻结，本环节侧重集成测试/Checker/性能测试。

前置条件：

1. 性能基线已在设计阶段确定（带宽/延迟目标）——否则T-4（测试结果自动分析）无基线可对比
2. 测试环境（多卡/多机）可获取——无物理设备则AI生成的测试脚本无法验证
3. 测试规范要求（功能、性能、长稳、整网）已明确——AI生成的测试用例必须基于规范要求，不得自行发明测试类别

红线：

1. 必须基于测试的规范要求（功能、性能、长稳、整网）来生成测试用例——AI生成的测试用例须覆盖规范定义的全部测试类别，不得遗漏
2. AI不得生成并发测试的时序断言——并发时序属本质复杂性
3. AI不得判断通信结果正确性（如AllReduce结果是否等于各rank输入之和）——通信语义验证需领域知识
4. AI生成的UT桩代码必须通过编译验证——LLM对私有API编译率预估<30%
5. AI生成的测试脚本中的参数组合需人工确认覆盖度——AI可能遗漏边界条件（如消息大小为0、卡数为1等）

度量指标：

- UT/LLT编码时间变化
- UT覆盖率达标情况
- 测试脚本中AI生成占比
- 性能退化的发现时间点（目标：从ST阶段左移到编码阶段）

### 上板验证

- 责任方：开发者
- 输入：编译产物 + 测试脚本
- 输出：验证结果

上板验证的本质困难在于硬件交互的不可替代性——物理设备获取（多卡/多机为稀缺资源）、NPU行为的不可预测性（温度/功耗/硬件故障）、端到端集成(HCCL+ACL+Runtime+Driver)、性能调优（带宽/延迟与物理拓扑强相关）。偶然部分(30%)集中在环境配置、配置文件准备、测试执行脚本、日志收集与整理。

与上游的衔接：消费编码环节的编译产物+测试环节的测试脚本。

前置条件：

1. 环境检查清单已建立（NPU型号/SDK版本/镜像版本/网络配置/RDMA参数）——V-1的基础
2. 编译环境与运行环境已确认一致——算子组教训：联调环境信息未提前对齐

红线：

1. AI对上板日志的分析仅作初步分类（环境问题vs功能问题）——功能问题的根因分析必须由人工完成
2. AI不得直接修改HCCL运行时参数（HCCL_SOCKET_IFNAME、RDMA配置等）来"修复"上板失败——可能掩盖真正的问题
3. 多机日志的因果推理（rank间消息丢失/乱序）不由AI完成——AI可做时间线重建，人工做因果分析

度量指标：

- 环境配置时间变化（预期-50%）
- 上板失败中"环境问题"vs"功能问题"的分类准确率

### 问题定位

- 责任方：开发者/问题接口人
- 输入：问题描述 + 日志
- 输出：问题原因 + 修正方案

问题定位是AI最难介入的环节。本质困难包括：Heisenbug（通信超时、死锁的触发条件含不可控因素）、分布式因果推理（多rank间消息丢失/乱序需从多节点日志重建因果链）、性能劣化分析（需理解NPU通信硬件物理特性）、历史知识（根因可能是数月前的设计决策）。

与上游的衔接：消费上板验证/测试环节发现的问题描述+日志。

前置条件：

1. 问题知识库已建立初版——否则AI无法做已知模式匹配
2. 多机日志已收集且时间戳已对齐——否则AI日志分析无输入

红线：

1. AI给出的根因候选（3-5个文件/函数）仅作参考
2. AI不得做分布式因果推理——多rank间的消息因果关系需人工从通信时间线中判断
3. AI不得给出"问题已定位"的最终结论——只能给出候选范围，人工确认
4. Heisenbug（通信超时、死锁等非确定性问题）不由AI定位——触发条件含不可控因素（硬件状态、网络抖动、线程调度）
5. AI定位无效时，本次case必须补充到问题知识库——闭环

度量指标：

- 问题定位时间变化
- AI定位出的问题占比（知识库命中率）
- 知识库条目增长速率

## 环境配置

从"读完指导书"到"开始实践"，需要做这些准备。

### Claude Code CLI

Claude Code是Anthropic的命令行AI编程工具，代码库理解、多文件编辑、终端命令执行。本指南中多数策略的执行载体——C-1骨架生成、R-1分层检视、D-2图表生成等都通过Claude Code落地。

安装：

```bash
# macOS / Linux
npm install -g @anthropic-ai/claude-code

# 验证
claude --version
```

基本使用：

```bash
cd /path/to/hccl-project    # 进入项目目录
claude                      # 启动交互式会话
```

关键配置文件：

| 文件                    | 作用                                                   |
| ----------------------- | ------------------------------------------------------ |
| `.claude/settings.json` | 项目级设置（允许的工具、MCP服务器等）                  |
| `CLAUDE.md`             | 项目级指令——AI每次会话自动读取，用于注入团队规范和约束 |
| `~/.claude/CLAUDE.md`   | 用户级全局指令                                         |

HCCL实践建议：

- 在项目CLAUDE.md中写入C1-C8铁律和AI编码禁区（表4），让AI每次会话自动遵守
- 配合C-5远程开发模式，Claude Code本地跑，编译验证走远端
- 对God Object文件，利用CLAUDE.md约束AI不得整体读取

官方文档：https://docs.anthropic.com/en/docs/claude-code

### Skills开发实践

Skills是Claude Code的能力扩展机制——一个SKILL.md文件就能为AI注入领域知识和专项工作流。vibe-review（R-1分层检视）和vibe-design（D-1设计模板）都以Skill形式实现。

Skill目录结构：

```
<skill-name>/
├── SKILL.md       # 核心：frontmatter + 指令正文（必需）
├── references/    # 参考资料（规则库、大型文档，按需加载）
├── scripts/       # 辅助脚本
└── docs/          # 补充文档
```

SKILL.md编写规范：

| 要素         | 规范                                                    |
| ------------ | ------------------------------------------------------- |
| frontmatter  | `name` + `description`（含触发词）+ 可选`allowed-tools` |
| description  | 列举触发短语，确保用户自然表达即可触发                  |
| 正文语气     | 祈使句为主（"分析…"、"生成…"、"检查…"）                 |
| 篇幅         | 1500-2000字——太短信息不足，太长稀释注意力               |
| 参考资料引用 | 正文中用Read指令按需加载references/下的文件             |

渐进式信息公开——Skill对AI暴露信息分三层，避免一次性灌入过多上下文（C5原则的工具级体现）：

1. metadata：frontmatter中的name+description → Claude Code据此决定是否触发
2. SKILL.md正文：核心流程和约束 → 触发后加载
3. references/+scripts/：详细规则库和辅助脚本 → 正文中按需加载

验证清单：

- [ ] description包含用户常用的触发短语
- [ ] 正文不超过2000字
- [ ] 大型参考资料放在references/下，正文中按需加载
- [ ] 本地测试：在Claude Code中用自然语言触发，验证是否命中

## 资产库

ai4hccl仓库是这份指南的配套工具集。主仓+子模块架构，每个子模块独立演进。

### 仓库架构

```
ai4hccl/                # 主仓：指导书 + 子模块聚合
├── guide.md            # 全流程指导书（本文档）
├── docs/               # 技术文档与分析报告
│   └── (hccl-docs submodule)
└── skills/             # AI Skills集合
    ├── vibe-design/    # 设计文档生成
    ├── vibe-review/    # 代码审查
    └── vibe-pr/        # CANN社区开发bot
```

各子模块与策略的对应关系：

| 子模块      | 定位                          | 对应策略                               | 适配度  |
| ----------- | ----------------------------- | -------------------------------------- | ------- |
| hccl-docs   | repo-dig分析报告、AI4SE论文   | 全文档的实证数据基础                   | —       |
| vibe-design | 设计文档生成Skill             | D-1设计文档结构化辅助、D-2图表自动生成 | 0%→待建 |
| vibe-review | 代码审查Skill（80条HCCL规则） | R-1分层检视Layer 1、R-4审查规则演化    | 90%     |
| vibe-pr     | 从issue到PR的全流程自动化     | C-1算子骨架生成、C-2小粒度迭代编码     | 部分    |

### 使用方法

快速开始（详见README.md）：

```bash
# 克隆主仓及所有子模块
git clone --recurse-submodules https://github.com/tsukiyokai/ai4hccl.git

# 安装vibe-review（npm发布）
npx @tsukiyokai/vibe-review --global

# vibe-design / vibe-pr需通过setup.sh安装
./setup.sh    # 软链接skills到~/.claude/skills/
```

在HCCL项目中的典型使用场景：

| 场景                      | 命令示例                                      | 对应策略 |
| ------------------------- | --------------------------------------------- | -------- |
| PR提交前AI自检            | `/vibe-review HEAD~1..HEAD`                   | R-1      |
| 审查PR的并发相关变更      | `/vibe-review https://github.com/.../pull/42` | R-1      |
| 根据需求文档生成设计骨架  | `/vibe-design`                                | D-1      |
| 从issue出发完成开发全流程 | `/vibe-pr`                                    | C-1, C-2 |

更新到最新版本：

```bash
git pull && git submodule foreach 'git checkout main && git pull'
```

### 贡献方法

每个子模块是独立仓库，贡献流程：Fork → 开发 → 向子仓提PR → 子仓合入后主仓更新指针。

本地调试Skill：

```bash
cd skills/<skill-name>         # 进入子仓目录
git checkout -b feature/xxx    # 创建开发分支
# 修改SKILL.md或references/下的文件
# 在Claude Code中测试触发和执行效果
git add . && git commit        # 提交变更
```

新增子仓：

```bash
git submodule add https://github.com/tsukiyokai/<repo>.git skills/<name>
git commit -m "Add <name> submodule"
```

HCCL社区共建建议：

- 优先贡献vibe-review的HCCL审查规则（R-4审查规则演化）——每次人工review发现AI遗漏的模式，都可提炼为规则
- vibe-design的HCCL专项模板是从0%到可用的关键缺口——并发设计规格、协议兼容性、状态机设计等章节待建

## 技术沉淀与工具矩阵

### 已有工具适配度

表11现有工具HCCL适配度

| 工具                    | 当前适配度 | 差距                     | 适配方向                                              |
| ----------------------- | ---------- | ------------------------ | ----------------------------------------------------- |
| vibe-review（代码检视） | 90%        | 10%（误报排除+跨层分析） | 补充HCCL特有误报模式，增加初始化/赋值检查(INIT-01~03) |
| vibe-design（设计文档） | 0%         | 100%（无HCCL专项支持）   | 新增HCCL设计模板（并发规格+协议兼容+状态机+性能设计） |

### 新建工具建议

SubAgent（上下文独立、流程复杂）：

- 测试矩阵/用例设计：输入设计文档，输出用例清单
- UT/ST测试代码生成：多步骤（读仓→匹配风格→生成代码→检查编译）
- 多机日志分析：收集→对齐→关联→标注

Skill（上下文耦合、事务独立）：

- PTA测试脚本生成：模板化程度高
- 精度/性能达标判定：知识注入型——注入双标杆、容差阈值
- HCCL错误处理模式：在编码上下文中注入标准模式
- 环境准备检查清单：模板化的环境检查

### 周报度量指标

表12 AI辅助研发度量指标体系

| 维度     | 指标                                                       |
| -------- | ---------------------------------------------------------- |
| 工具沉淀 | 新增/完善Skills数量，知识库条目增长                        |
| 编码效率 | AI生成代码行数/总代码行数，编译通过率                      |
| 质量影响 | CI拦截的缺陷数（CMake守卫+编译器诊断），AI检视发现的问题数 |
| 定位效率 | 问题知识库命中率，AI辅助定位成功率                         |

## 附录

### AgenticSE四大悖论与HCCL缓解策略

这四个悖论来自AgenticSE研究，听着学术，但每个都在HCCL的实践中真实存在。用大白话说：

表14 AgenticSE悖论在HCCL中的表现及缓解

| 悖论              | 大白话                                   | 在HCCL中的表现                     | 我们怎么应对                              |
| ----------------- | ---------------------------------------- | ---------------------------------- | ----------------------------------------- |
| Eagerness Paradox | AI越快越容易跑偏——它不会停下来想"我对吗" | 快速生成的并发代码可能引入竞态条件 | C-2小粒度迭代 + Beyond Done（TSan验证）   |
| Context Paradox   | 给太多上下文反而更糟——AI被信息淹没了     | 65K LOC不能全部输入AI              | C-3 God Object上下文管理（分区+方法索引） |
| Tunnel Vision     | 局部完美但破坏全局——AI只看眼前这片代码   | 改executor可能破坏跨层通信语义     | R-2 Delta-first Review（影响范围分析）    |
| Learning Paradox  | AI不积累经验——每次都从零开始             | 并发bug pattern需要持久化          | R-4审查规则演化 + P-1问题知识库           |

Eagerness Paradox是最危险的。AI写了一段并发代码，信誓旦旦说"线程安全"——一跑TSan，竞态条件满天飞。AI的问题不是不够聪明，而是太自信。所以我们要求：每次并发代码变更必须附TSan结果，AI说安全不算数。

Context Paradox在God Object上体现得淋漓尽致。communicator_host.cc有9152行，全量输入AI后生成质量反而下降。切200-500行精准上下文，效果更好。违反直觉，但这是实测结论。

Tunnel Vision在HCCL特别致命。ops里改一个kernel，影响范围就那个kernel。HCCL里改一个executor的逻辑，可能影响selector的策略选择，进而影响API的语义。局部看没问题，全局看出大事了。所以P0变更必须做Delta-first Review。

Learning Paradox意味着不能指望AI"学会"上次的经验。每次人工review发现AI漏掉的缺陷，必须编码成持久化规则（vibe-review规则库或checklist），否则下次AI还是会漏。

### HCCL缺陷模式速查

数据来源：repo-dig深度分析（HCCL 84条缺陷/428次提交，HCOMM 162条缺陷/3084源文件）。

表15 HCCL缺陷分类及AI可检测度

| 缺陷类别     | HCCL频次        | HCOMM频次 | 严重度 | AI可检测度                       |
| ------------ | --------------- | --------- | ------ | -------------------------------- |
| 算法正确性   | 21(25.0%)       | —         | P0-P1  | 中（单线程逻辑可检测，并发不可） |
| 配置与兼容性 | 13(15.5%)       | —         | P0-P1  | 中高（规则化检测）               |
| 并发问题     | 10(11.9%)       | 11(6.8%)  | P0     | 低（40%可规则化，20%需运行时）   |
| 日志与调试   | 8(9.5%)         | —         | P2     | 高（格式串匹配等确定性检查）     |
| 资源生命周期 | 8(9.5%)         | —         | P1     | 中高（模式化检测）               |
| 错误处理     | 7(8.3%)         | —         | P1     | 高（返回值检查等模式明确）       |
| 缓存一致性   | 6(7.1%)         | —         | P1     | 低（需理解业务语义）             |
| 构建/链接    | 3(hccl-dev 30%) | 24(14.8%) | P2     | 高（CMake检查+编译器诊断）       |

几个值得注意的点：

- 算法正确性缺陷占25%，是HCCL最大的缺陷类。但其中涉及并发的部分AI检测不了。
- 并发问题只占11.9%的频次，但严重度全是P0。10条里每一条都可能是生产事故。
- 构建/链接类在HCOMM中占14.8%（频次最高），大部分可以被B-1和B-2拦截——这就是为什么我们说构建是ROI最高的环节。
- 日志和错误处理类AI检测度最高，这正是vibe-review Layer 1的覆盖重点。

## 参考文献

[1] Brooks, F.P. "No Silver Bullet — Essence and Accident in Software Engineering." Proc. IFIP Tenth World Computing Conference, 1986. Reprinted in IEEE Computer, 20(4), 10-19, 1987.

[2] Hassan, A.E. Agentic Software Engineering: Building Trustworthy Software with Stochastic Teammates at Unprecedented Scale. 2026.

[3] Abdullin, A., Derakhshanfar, P., Panichella, A. "Test Wars: A Comparative Study of SBST, Symbolic Execution, and LLM-Based Approaches to Unit Test Generation." ICST 2025. arXiv:2501.10200.

[4] Bogomolov, E. et al. "Long Code Arena: a Set of Benchmarks for Long-Context Code Models." arXiv:2406.11612, 2024.

[5] repo-dig: CANN代码仓系统性缺陷挖掘（内部分析）。覆盖：HCCL主仓84条缺陷/428次提交，HCOMM 162条缺陷/3084源文件，HCCL-dev 10条缺陷，8个跨仓共性模式，258条审查规则.

[6] conv算子组AI辅助NPU算子开发实战复盘（内部报告）。最终代码约70%以上由AI生成，6条核心教训.

补充参考：AI4SE论文集共16篇(TU Delft AI4SE Lab + JetBrains Research, 2024-2026)，其中[3][4]直接引用数据，其余为方法论参考。完整列表见 https://se.ewi.tudelft.nl/ai4se/publications.html

## 修订记录

| 版本 | 日期       | 修订内容                                                                                                                                                                                                      |
| ---- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v0.1 | 2026-03-09 | 初稿完成，含7章27条策略                                                                                                                                                                                       |
| v1.0 | 2026-03-09 | 补充行内引用与参考文献；格式正式化                                                                                                                                                                            |
| v1.1 | 2026-03-10 | 增加逐环节约束分析：1.4节增加跨环节通用约束(C1-C8); 2.1-2.8各环节增加约束框（前置条件+红线+产出格式约束+环节衔接）；新增2.9节环节衔接总览（含约束间张力分析）；更新表10整合流程约束（C7/C8/误报闭环/TDD冻结） |
| v1.2 | 2026-03-10 | 新增环境配置章节（Claude Code CLI配置指引 + Skills开发实践）和资产库章节（仓库架构、使用方法、贡献方法）；更新跨章节引用                                                                                      |
| v2.0 | 2026-03-10 | 全文重构：从学术论文风格改写为实战指南风格；技术内容和数据不变，表达更直接；重写AgenticSE四大悖论解读                                                                                                         |
